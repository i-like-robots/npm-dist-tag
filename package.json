{
  "name": "npm-dist-tag",
  "version": "1.0.0",
  "description": "A tool to find the appropriate npm distribution tag when publishing a new version of your package.",
  "bin": {
    "npm-dist-tag": "bin/npm-dist-tag.js"
  },
  "main": "lib/index.js",
  "scripts": {
    "lint": "prettier --config .prettierrc.json --list-different lib/*.js",
    "test": "npm run lint && echo 'no tests defined yet'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "minimist": "^1.2.0",
    "node-fetch": "^2.6.0",
    "semver": "^7.1.0"
  },
  "engines": {
    "node": ">= 10.0.0"
  },
  "devDependencies": {
    "prettier": "^1.19.1"
  }
}
